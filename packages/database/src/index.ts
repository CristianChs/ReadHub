// Contrato del cliente Supabase tipado. NO incluye los clientes de Next
// (browser/server/middleware): esos son específicos de la app web porque
// dependen de `next/headers` y `next/server`.
export * from "./client";
