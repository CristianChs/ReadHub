import { notImplemented } from "@/lib/api/responses";

// GET/PUT/DELETE /api/v1/articles/{id} — pendiente (services/article.service).
export const GET = () => notImplemented();
export const PUT = () => notImplemented();
export const DELETE = () => notImplemented();
