import { notImplemented } from "@/lib/api/responses";

// GET/POST /api/v1/articles — pendiente (lógica en services/article.service).
export const GET = () => notImplemented();
export const POST = () => notImplemented();
